<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:App1"
             xmlns:viewModels="clr-namespace:App1.ViewModels;assembly=App1"
             x:Class="App1.Views.MainPage">
    <ContentPage.BindingContext>
        <viewModels:MainViewModel/>
    </ContentPage.BindingContext>

    <StackLayout>
        <Image Source="logo_food_tracker" Margin="16"/>
        <!-- Place new controls here -->
        <SearchBar x:Name="SearchBar" SearchButtonPressed="Button_OnClicked" Placeholder="Enter product code" Text="{Binding SearchText}" Margin="24"/>
        <Button Text="Search" Clicked="Button_OnClicked" MinimumWidthRequest="10" Margin="24"  CornerRadius="20"></Button>
    </StackLayout>

</ContentPage>
